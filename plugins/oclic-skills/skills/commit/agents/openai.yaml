interface:
  display_name: "Commit"
  short_description: "Create focused, categorized local commits"
  default_prompt: "Use $commit to review and categorize my current changes into focused local commits."
